@page
@model Zealand_Lokale_Booking_UI.Pages.MyBookingsModel
@{
    ViewData["ShowBookingBookRoomLink"] = true;    
}

<div class="container-fluid mt-4">
    <div class="row">

        <!-- TABEL -->
        <div class="col-12 col-md-9 mt-4 mt-md-0 align-middle">
            <div class="table-responsive shadow-sm rounded align-middle">
                <table class="table table-bordered align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>Lokale</th>
                            <th>Afdeling</th>
                            <th>Bygning</th>
                            <th>Etage</th>
                            <th>Type</th>
                            <th>Tid</th>
                            <th>Book</th>
                            <th>Slet</th>
                        </tr>
                    </thead>
                    <tbody>
@*                         @foreach (var b in Model.AvailableBookings)
                        {
                            <tr>
                                <td>@b.RoomName</td>
                                <td>@b.DepartmentName</td>
                                <td>@b.BuildingName</td>
                                <td>@b.Level</td>
                                <td>@b.RoomType</td>
                                <td>@b.StartTime.ToString(@"hh")-@b.StartTime.Add(TimeSpan.FromHours(2)).ToString(@"hh")</td>
                                <td>
                                    <button type="button"
                                            class="btn btn-primary btn-sm w-100"
                                            onclick="openBookingModal(this)"
                                            data-room-id="@b.RoomID"
                                            data-room-name="@b.RoomName"
                                            data-department-name="@b.DepartmentName"
                                            data-building-name="@b.BuildingName"
                                            data-date="@b.Date.ToString("yyyy-MM-dd")"
                                            data-time="@b.StartTime.ToString(@"hh\:mm")-@b.StartTime.Add(TimeSpan.FromHours(2)).ToString(@"hh\:mm")">
                                        Book
                                    </button>
                                </td>
                            </tr>
                        } *@
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
